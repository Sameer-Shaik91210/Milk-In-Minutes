export type order = {
  productName?: string;
  productPrice?: number;
  productCategory?: string;

  phone: string;
  email: string;

  name: string;

  houseNoStreet: string;
  cityState: string;
  zipcode: string;
  quantity: number;
  totalPrice: number;
};
